<UserControl x:Class="WorkoutCoachV2.App.View.ExercisesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Zoek:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Grid.Column="1"
                     Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"/>

            <Button Grid.Column="2" Margin="8,0,0,0" Width="100"
                    Content="Vernieuwen" Command="{Binding RefreshCmd}"/>
            <Button Grid.Column="3" Margin="8,0,0,0" Width="100"
                    Content="Nieuw" Command="{Binding AddCmd}"/>
            <Button Grid.Column="4" Margin="8,0,0,0" Width="100"
                    Content="Bewerken" Command="{Binding EditCmd}"/>
            <Button Grid.Column="5" Margin="8,0,0,0" Width="100"
                    Content="Verwijderen" Command="{Binding DeleteCmd}"/>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Export Exercises"
                    Padding="12,6"
                    Margin="0,0,8,0"
                    Command="{Binding ExportExercisesCommand}" />
            <Button Content="Import Exercises"
                    Padding="12,6"
                    Command="{Binding ImportExercisesCommand}" />
        </StackPanel>

        <DataGrid Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding Selected, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Naam" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="Categorie" Binding="{Binding Category}" Width="*"/>
                <DataGridTextColumn Header="Aangemaakt" Binding="{Binding CreatedAt}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
