<Window x:Class="WorkoutCoachV2.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:v="clr-namespace:WorkoutCoachV2.App.View"
        Title="WorkoutCoach" Height="520" Width="860"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <DockPanel LastChildFill="True">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="8" VerticalAlignment="Center">
            <TextBlock Text="{Binding Greeting}" FontWeight="Bold" Margin="0,0,12,0"/>
            <Button Content="Logout" Width="80" Click="Logout_Click"/>
        </StackPanel>

        <TabControl>
            <TabItem Header="Exercises"
                     Visibility="{Binding CanSeeExercises, Converter={StaticResource BoolToVis}}">
                <v:ExercisesView/>
            </TabItem>

            <TabItem Header="Workouts"
                     Visibility="{Binding CanSeeWorkouts, Converter={StaticResource BoolToVis}}">
                <v:WorkoutsView/>
            </TabItem>

            <TabItem Header="Sessions"
                     Visibility="{Binding CanSeeSessions, Converter={StaticResource BoolToVis}}">
                <v:SessionsView/>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
