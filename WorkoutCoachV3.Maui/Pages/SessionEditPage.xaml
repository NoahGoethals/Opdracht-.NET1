<?xml version="1.0" encoding="utf-8" ?>
<!-- Session edit: basisgegevens + workouts selecteren -->
<ContentPage
    x:Class="WorkoutCoachV3.Maui.Pages.SessionEditPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="{Binding Title}">

    <!-- Scrollbaar formulier binnen padding -->
    <Grid Padding="16">
        <ScrollView>
            <VerticalStackLayout Spacing="10">

                <!-- Titel van de sessie -->
                <Label Text="Title" FontAttributes="Bold" />
                <Entry Text="{Binding SessionTitle}" Placeholder="Session title..." />

                <!-- Datum kiezen -->
                <Label Text="Date" FontAttributes="Bold" />
                <DatePicker Date="{Binding SessionDate}" />

                <!-- Optionele beschrijving -->
                <Label Text="Description" FontAttributes="Bold" />
                <Editor Placeholder="Optional..."
                        AutoSize="TextChanges"
                        HeightRequest="90"
                        Text="{Binding Description}" />

                <!-- Workouts selecteren om te linken aan sessie -->
                <Label Text="Select workouts"
                       FontAttributes="Bold"
                       Margin="0,6,0,0"/>

                <CollectionView ItemsSource="{Binding Workouts}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnDefinitions="Auto,*" Padding="8">
                                <CheckBox IsChecked="{Binding IsSelected}" />
                                <Label Grid.Column="1"
                                       Text="{Binding WorkoutTitle}"
                                       VerticalTextAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- Save/Cancel acties -->
                <Grid ColumnDefinitions="*,*" Margin="0,12,0,0">
                    <Button Text="Save" Command="{Binding SaveCommand}" />
                    <Button Grid.Column="1" Text="Cancel" Command="{Binding CancelCommand}" />
                </Grid>

                <!-- Error melding uit ViewModel -->
                <Label Text="{Binding Error}" TextColor="Red" />

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
